#
'''
TODO write a function here that sets up the buttons, and if they're both held then deletes config.json and turns the lights on in a while true.

The user can then plug the device in whilst holding both buttons to factory reset, and the LEDs locking on will prove its worked. They can then powercycle again for a normal boot

'''